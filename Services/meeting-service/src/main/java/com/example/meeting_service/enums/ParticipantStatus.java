package com.example.meeting_service.enums;

public enum ParticipantStatus {
    INVITED,    // Invité (pas encore répondu)
    ACCEPTED,   // A accepté
    DECLINED,   // A refusé
    ATTENDED    // A participé (rejoint la réunion)
}
